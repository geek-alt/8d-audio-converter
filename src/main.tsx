import React from 'react'
import ReactDOM from 'react-dom/client'
import AudioConverter from './8d_audio_converter'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <AudioConverter />
    </React.StrictMode>,
)
